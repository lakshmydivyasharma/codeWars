//convert a string to an stringToArray
//Write a function to split a string and convert it into an array of words. For example:

// i pulled this out of my palindrome code i solved with Gus the other day; but i shared it with victor, shane, illunga while solving this problem as a group 


function stringToArray(string){
{
     return string.trim().split(" ");
};

}
